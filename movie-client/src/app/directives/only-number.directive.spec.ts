import { OnlyNumberDirective } from './only-number.directive';

describe('PreventPasteDirective', () => {
  it('should create an instance', () => {
    const directive = new OnlyNumberDirective();
    expect(directive).toBeTruthy();
  });
});
